#include <iostream>
#include <string>

using namespace std;

class Game;
class Player;
int main()
{
    std::string name1;
    std::string name2;
    std::string str;
    int score1;
    int score2;

    std::getline(ifs,name1,';');
    std::getline(ifs,name1,'\n');
    score1=atoi(str.c_str());

    std::getline(ifs,name2,';');
    std::getline(ifs,name2,'\n');
    score2=atoi(str.c_str());

    return 0;
}
